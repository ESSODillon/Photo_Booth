# Port for the Express server
PORT=5000

# Path where photos should be saved. Defaults to ../photos relative to server
# PHOTOS_DIR=/absolute/path/to/photos

# Command used to trigger the camera (gphoto2 recommended)
CAMERA_CMD=gphoto2

# Enable to generate placeholder images instead of using the real camera
# Useful for development on machines without gphoto2 installed.
USE_MOCK_CAMERA=true

